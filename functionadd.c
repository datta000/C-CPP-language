#include <stdio.h>
#include <conio.h>
int sum();
int main()
{
    int a, b, x;
    printf("enter two number a and b");
    scanf("%d%d", &a, &b);
    x = sum(a, b);
    printf("%d", x);
}
int sum(int p, int q)
{
    int p,q,sum;
    int y;
    y = p + q;
}