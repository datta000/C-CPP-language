#include<iostream>
int insert(int arr[],int c,int index, int element,int size)
{
    if(c<=size){
        return 0;
    }
    else{
        for(int i=size-1;i>=index;i--){
            arr[index]=element;
            return 1;
        }
    }
    void main()
    {
        int c,size,element,index,arr[];
        std::cout<< "enter the size of the array";
        std::cin>>c;
        std::cout
        
        cout<<"enter the elements of the array"
    }
}